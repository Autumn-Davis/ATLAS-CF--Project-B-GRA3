function setup() 
{
	createCanvas(400, 400);
}

function draw()
{
    background (220);
    stroke('white');
    ellipse(width/2, height * 0.8, 100, 120);
    ellipse(width/2, height * 0.6, 80, 70);
}
